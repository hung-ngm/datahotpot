import { Layout } from '../src/components/layout';
import { UploadDataset } from '../src/components/templates/uploadDataset';

const UploadDatasetPage = () => {
    return (
        <Layout>
            <UploadDataset/>
        </Layout>
    );
};

export default UploadDatasetPage;