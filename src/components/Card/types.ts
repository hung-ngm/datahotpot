export type TCard = {
    className: string;
    item: {
        category: string,
        categoryText: string,
        url: string,
        title: string,
        price: string,
        highestBid: string,
        counter: string,
        bid: string, 
        image: string, 
        image2x: string,
        avatar: string,
    }
}
